vps = vps || {};

/**
 * Construct a Camera
 */
vps.Camera = function(position, rotation, viewerPosition){
	
	this.position = position;
	this.rotation = rotation;
	this.viewerPosition = viewerPosition;
};